Una vez terminado ese paso en mi caso he creado una aplicación llamada empleados por ejemplo y ahora tenemos que añadir la aplicación a la lista de aplicaciones instaladas en el archivo settings.py. Esto se hace para que Django sepa que la aplicación existe. Para ello, escribimos:

```python

 INSTALLED_APPS = [
     'django.contrib.admin',
     'django.contrib.auth',
     'django.contrib.contenttypes',
     'django.contrib.sessions',
     'django.contrib.messages',
     'django.contrib.staticfiles',
     'empleados',
 ]

```

Creamos una clase en models.py para crear la tabla de la base de datos. Para ello, escribimos:

```python

 from django.db import models

 class Empleado(models.Model):
     nombre = models.CharField(max_length=50)
     apellidos = models.CharField(max_length=50)
     email = models.EmailField()
     edad = models.IntegerField()
     telefono = models.CharField(max_length=9)

```

Creamos la base de datos en MySQL. Para ello, escribimos:

```bash

 mysql -u root -p

```

Y creamos la base de datos. Para ello, escribimos:

```sql

 CREATE DATABASE crud;

```

Y ahora tenemos que configurar la base de datos en el archivo settings.py. Para ello, escribimos:

```python

 DATABASES = {
     'default': {
         'ENGINE': 'django.db.backends.mysql',
         'NAME': 'crud',
         'USER': 'root',
         'PASSWORD': 'root',
         'HOST': 'localhost',
         'PORT': '3306',
     }
 }

```

Y debemos crear las migraciones. Para ello, escribimos:

```bash

 python manage.py makemigrations
 python manage.py migrate

```

Esto se hace para que Django cree las tablas en la base de datos.